import { DefaultTheme } from 'styled-components'

const primary: DefaultTheme = {
  bg: '#F6F6F6',
  fg: '#FFFFFF',
  fontFamily: {
    default: 'sans',
    secondary: 'sans',
  },
}

export default primary
